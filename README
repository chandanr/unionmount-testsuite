To run these tests:

 (1) If testing unionmount, do:

	./run --um

     If testing overlayfs, do:

	./run --ov[=N]

     Where N is the max number of layers to recycle

     If testing overlayfs snapshots, do:

	./run --sn[=N]

     Where N is the max number of snapshots to recycle

     To run the tests without any unioning, do:

	./run --no

     To just set up the environment and exit without doing any tests,
     add the --set-up flag, eg:

	./run --ov --set-up
